#pragma once

// Run `ipconfig` (Windows) and use the IPv4 address of your WiFi adapter
#define MQTT_BROKER      "192.168.1.9"
#define MQTT_PORT        1883

#define DEVICE_ID        "feather-01"
#define READ_INTERVAL_MS 1000
